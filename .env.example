# Copy to .env.local and fill in your own values.
# Never commit a real bearer token.

VITE_API_BASE_URL=https://hrms.nsegindia.com
VITE_USE_PROXY=true
VITE_ORGANIZATION_ID=1

# Vercel serverless proxy only (set in Vercel project env, not needed locally when using Vite proxy):
# BACKEND_URL=https://hrms.nsegindia.com
